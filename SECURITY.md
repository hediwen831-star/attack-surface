# 安全政策

## 这个项目的特殊性

ASP 是一个**主动扫描工具** —— 它会向目标发起真实请求、抓取 banner、执行 PoC 验证。
所以「项目自身的安全」有两层含义：

1. **工具代码本身的安全**（本文件主要讲这个）
2. **工具被滥用的风险**（见下方「责任边界」）

---

## 工具的安全设计

| 机制 | 位置 | 说明 |
|---|---|---|
| Web 接口默认只绑回环 | `asp/api/app.py` | 外部访问不到 |
| 绑非回环时强制要求 token | `check_bind_safety()` | 没设 token 直接**拒绝启动**，不是打印警告 |
| 扫描端点要求 `X-API-Token` | `make_token_dependency()` | 只读端点不强制（看报告不产生流量） |
| token 恒定时间比较 | `hmac.compare_digest` | 避免通过响应时间侧信道爆破 |
| PoC DSL 白名单 | `asp/plugins/matchers.py` | **不支持任意代码** —— 加载第三方 PoC ≠ 执行任意代码 |
| 负向对照校验 | `asp/plugins/engine.py` | 降低误报，避免把通用页面当成漏洞 |
| Docker 内非 root 运行 | `Dockerfile` | 处理外部数据的程序不该以 root 跑 |

**其中最值得强调的是 PoC DSL 沙箱。**

一个「能从网上加载 YAML 并执行」的引擎，如果支持任意代码求值，
那它本身就是 RCE 入口 —— 使用者以为在扫别人，实际被人扫。
所以这里用白名单表达式（只允许比较、包含、正则等有限操作），而不是 `eval`。
`tests/test_matchers.py` 里有一条 `test_dsl_rejects_non_whitelisted_syntax`
专门守着这条线。

---

## 责任边界（重要）

**本工具不判断授权状态。**

它能对任意目标发起扫描，但「有没有权限扫」完全由使用者负责。

这不是免责声明式的推诿 —— 而是这类工具的固有性质：
**工具无法知道你与目标之间的法律关系。**

使用前请确认：

- 你拥有目标的书面测试授权
- 授权范围**包含主动扫描**（很多授权只允许被动信息收集）
- 你的测试时间窗口与目标方约定一致

README 与 CLI 的 `--help` 里都有对应提示。

---

## 报告漏洞

如果发现**工具本身**的问题，请报告：

| 类型 | 例子 |
|---|---|
| PoC DSL 沙箱逃逸 | 构造的 YAML 能执行任意代码 |
| Web 接口鉴权绕过 | 未带 token 也能触发扫描 |
| 路径穿越 | 配置或规则文件路径能读到系统文件 |
| 工具侧 SSRF | 被扫描的目标能反向影响扫描器 |
| 依赖漏洞 | 运行时依赖的已知 CVE |

请通过 GitHub 的 [Private vulnerability reporting](../../security/advisories/new)
提交，或邮件 hediwen831@gmail.com。

**请不要开公开 issue。**

---

## 支持范围

| 版本 | 支持状态 |
|---|---|
| `main` 分支最新提交 | ✅ 接受报告 |
| 历史提交 | ❌ 请先确认能在最新 `main` 上复现 |
